# Day 4 Report – DevOps Track

## ◈ Technical Summary
Today I learned about Infrastructure as Code using shell scripting and YAML. I automated server setup using a script instead of manual steps.

## ◈ Bug Log
I faced an issue with YAML indentation. It was not working properly. I fixed it by using proper spacing instead of tabs.

## ◈ Conceptual Reflection
Shell script (.sh) is used to perform actions like creating folders and running commands. YAML (.yaml) is used to define configurations.

Using code instead of manual setup is helpful because it saves time, reduces errors, and allows easy setup for multiple systems.